name: Workflow with Env Context.
on:
  push:
    branches:
      - main
env:
  name: hassan
  env_type: workflow level env

jobs:
  evn-context-job:
    runs-on: ubuntu-latest
    steps:
    
    - name: working with env variables
      run: |
        echo: "my name is ${{env.name}}"
        echo: "This is ${{env.env_type}}"
